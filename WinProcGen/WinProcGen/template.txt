{class}_#begin
/*
    Generated windows procedure for {class}
*/
LRESULT CALLBACK {class}_WndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam)
{
    switch (message)
    {

WORD {class}_OnPaint({class}* p, HDC hdc);
    case WM_PAINT:
        {
            PAINTSTRUCT ps;
            HDC hdc = BeginPaint(hWnd, &ps);
            {class}_OnPaint(p, hdc);
            EndPaint(hWnd, &ps);
        }

WORD {class}_OnDestroy({class}* p);
    case WM_DESTROY:
        {
            {class}_OnDestroy(p);
        }

WORD {class}_OnCommand({class}* p);
    case WM_COMMAND:
        {
            {class}_OnCommand(p);
        }

WORD {class}_OnClose({class}* p);
  case WM_CLOSE:
    {class}_OnClose();

{class}_#end
    }
  }
